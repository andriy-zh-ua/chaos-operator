# This rule is not used by the project chaos-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the chaos.a2solutions.ca.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: chaos-operator
    app.kubernetes.io/managed-by: kustomize
  name: disruption-editor-role
rules:
- apiGroups:
  - chaos.a2solutions.ca
  resources:
  - disruptions
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - chaos.a2solutions.ca
  resources:
  - disruptions/status
  verbs:
  - get
