---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: disruptions.chaos.a2solutions.ca
spec:
  group: chaos.a2solutions.ca
  names:
    kind: Disruption
    listKind: DisruptionList
    plural: disruptions
    singular: disruption
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Disruption is the Schema for the disruptions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Disruption
            properties:
              podKill:
                description: PodKill configuration for chaos experiments
                properties:
                  count:
                    description: |-
                      Count is only respected when KillMode=fixed-count.
                      Count specifies the number of pods to kill.
                    format: int32
                    minimum: 1
                    type: integer
                  duration:
                    description: Duration for which the disruption should last (e.g.,
                      5m, 30s)
                    type: string
                  gracePeriodSeconds:
                    description: |-
                      GracePeriodSeconds specifies the grace period for pod before termination.
                      If not specified, the pod's default grace period will be used.
                    format: int64
                    minimum: 0
                    type: integer
                  killMode:
                    default: random
                    description: KillMode specifies how to select pods for killing
                    enum:
                    - random
                    - all
                    - fixed-count
                    type: string
                  namespaces:
                    description: |-
                      Namespaces is only respected when Scope=cluster.
                      If empty, targets all non-system namespaces.
                    items:
                      type: string
                    type: array
                  scope:
                    default: namespace
                    description: Scope defines the scope of the disruption (namespace
                      or cluster)
                    enum:
                    - namespace
                    - cluster
                    type: string
                  selector:
                    description: Selector defines the label selector to identify which
                      pods to target
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - killMode
                - scope
                type: object
              safety:
                description: Safety configuration to prevent excessive disruption
                properties:
                  countLimit:
                    format: int32
                    type: integer
                  durationSeconds:
                    format: int32
                    type: integer
                  gracePeriodSeconds:
                    format: int64
                    type: integer
                  monitoringReconcileInterval:
                    description: |-
                      A Duration represents the elapsed time between two instants
                      as an int64 nanosecond count. The representation limits the
                      largest representable duration to approximately 290 years.
                    format: int64
                    type: integer
                  percentageAffected:
                    format: int32
                    type: integer
                  podsAffected:
                    format: int32
                    type: integer
                type: object
            type: object
          status:
            description: status defines the observed state of Disruption
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Disruption resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endTime:
                description: endTime represents when the disruption completed or failed
                format: date-time
                type: string
              lastExecution:
                description: lastExecution represents the timestamp of the last execution
                  attempt
                format: date-time
                type: string
              phase:
                description: phase represents the current phase of the disruption
                type: string
              podsAffected:
                description: podsAffected represents the number of pods affected by
                  the disruption
                format: int32
                type: integer
              startTime:
                description: startTime represents when the disruption started execution
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
